import { JWTBody, JWTToken, EncodingOptions, DecodingOptions, EncodingKey } from '../types/jwt';
declare const JWT: {
    encode: (body: JWTBody, key: EncodingKey, options?: EncodingOptions) => string;
    decode: (token: JWTToken, key: EncodingKey, options?: DecodingOptions) => Record<string, any>;
};
export default JWT;
